# 2026-03-31

## 今日完成

- 从空目录搭起了 `Next.js + Tailwind + Framer Motion` 的动态路书项目骨架。
- 实现了 28 天行程解析、Dashboard、Timeline、地图面板、疲劳指数计算。
- 增加了景点展示卡片，并接入了一批 Wikimedia Commons 可复用图片与署名信息。
- 增加了 `Crew Manifest` 功能。
  - Day 1-17：你一个人
  - Day 18-20：你 + 妈妈
  - Day 21-28：你 + 妈妈 + 爸爸
- 增加了头像资源：
  - 你：大便头像
  - 妈妈：正常头像
  - 爸爸：正常头像
- 时间线交互从“滚动误切换”改成了更稳的点击切换，并增加 `Prev Day / Next Day`。
- 高德地图 JSAPI 已接入代码，并支持 `.env.local` 里的：
  - `NEXT_PUBLIC_AMAP_KEY`
  - `NEXT_PUBLIC_AMAP_SECURITY_CODE`
- 修复了高德地图 `Polyline path` 报错，增加了坐标合法性过滤。
- 初始化了 git 仓库，并推送到 GitHub：
  - `https://github.com/mythfoxtang/dynamic-roadbook`

## 当前文件重点

- `components/dashboard.js`
- `components/map-panel.js`
- `components/timeline.js`
- `lib/roadtrip-data.js`
- `lib/place-data.js`
- `lib/parser.js`
- `lib/crew-data.js`
- `lib/photo-credits.js`
- `.env.example`
- `.gitignore`

## 当前问题

### 1. 数据编码污染

- 若干中文数据文件在终端显示时已经出现编码污染。
- 这会影响地点名、标题文本以及部分地图点位匹配。
- 后续最好直接重写一份干净、规范化的数据源，而不是继续在现有污染文本上修补。

### 2. 图片质量还不够好

- 已经接入真图，但用户明确反馈“图片不够漂亮”。
- 后续应优先优化这些节点的图片质量：
  - 冈仁波齐
  - 珠峰大本营
  - 赛里木湖
  - 夏塔
  - 独库段 / 慕士塔格

### 3. 临时公网链接不稳定

- Cloudflare Quick Tunnel 多次失败：
  - `500 Internal Server Error`
  - `error code: 1101`
- localhost.run 也拿到过域名，但实际公网访问不稳定。
- 结论：临时 tunnel 今天不可靠，不建议继续把它当正式方案。

### 4. Vercel 账号注册审核中

- Vercel 没法正常登录，进入注册审核。
- support case number：
  - `01073458`
- 邮件说明典型审核时间：
  - `24 小时 / 1 个工作日`
- 当前不应继续反复登录或重复提交。

## 本地运行状态

- 本地生产服务曾稳定运行于：
  - `http://127.0.0.1:3010`
- 开发服务曾跑在：
  - `http://localhost:3002`
- 但这些都是临时运行态，关机后失效。

## 明日优先事项

### A. 如果 Vercel 审核通过

1. 登录 Vercel
2. 导入 GitHub 仓库：
   - `mythfoxtang/dynamic-roadbook`
3. 在 Vercel 配置环境变量：
   - `NEXT_PUBLIC_AMAP_KEY`
   - `NEXT_PUBLIC_AMAP_SECURITY_CODE`
4. 获取线上域名
5. 去高德控制台把线上域名加入 `Web端(JSAPI)` 白名单

### B. 代码优先修复

1. 重写干净的中文路线/地点数据源
2. 检查并稳定地图点位逻辑
3. 换更漂亮的关键景点图片
4. 视情况补 `README.md` 和部署说明

## 下次打开时可直接说

```text
继续 dynamic-roadbook 项目，先看仓库里的 2026-03-31.md。
仓库：https://github.com/mythfoxtang/dynamic-roadbook

当前重点：
1. 等 Vercel 审核结果，case 01073458
2. 审核通过后部署
3. 重写被编码污染的数据源
4. 优化地图
5. 换更漂亮的图片
```
