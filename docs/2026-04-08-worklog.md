# 2026-04-08 工作记录

## 今天完成的内容

1. 重构了产品入口。
   - 首页改成模块入口页。
   - 拆成 `行程规划` 和 `动态路书` 两个模块。
   - 现有路书迁到 `/roadbook`。

2. 搭好了行程规划前端骨架。
   - 新增 `/planner` 页面。
   - 明确了 4 类用户入口：
     - 已有详细计划
     - 已有想法，未成行程
     - 只确定想去的地方
     - 只有旅行需求，未定目的地
   - 补了对应的前端引导文案和 PRD 草稿。

3. 补了“住宿成本感知”的产品原型。
   - 在规划阶段展示住宿价格会如何影响路线决策。
   - 明确了“规划阶段查价格，路书阶段做预订执行”的产品分层。

4. 做了路书侧的预订执行台。
   - 支持酒店、交通、租车、门票四类任务。
   - 支持本地状态回填：
     - 已去预订
     - 已预订
     - 订单号
     - 备注
   - 首页增加了预订总览。

5. 做了携程酒店自动化实验。
   - 新增本机 Playwright 脚本，用于打开携程并尝试自动填写酒店搜索条件。
   - 复用了持久化浏览器资料目录，便于保留登录态。
   - 结论：自动化可以继续研究，但不适合作为当前主路径。

6. 做了酒店价格探针原型。
   - 新增本地 API 和脚本链路。
   - 能从规划页触发酒店价格探针实验。
   - 结论：价格感知方向是对的，但真实稳定抓取还需要继续打磨。

7. 做了路书推荐能力的第一轮调整。
   - 放弃了静态“我替用户推荐”的做法。
   - 改成高德候选搜索思路。
   - 当前保留了路书工作台里的 `推荐` 面板，不再把沿线推荐强塞进地图主链路。

8. 恢复了地图主链路稳定性。
   - 撤回了“沿线推荐直接打到地图上”的实验。
   - `map-panel.js` 已恢复到稳定版本。
   - 当前地图应回到实验前状态。

## 当前项目状态

- 首页模块入口：已完成
- 行程规划页面：已完成基础版本
- 动态路书主页面：可用
- 路书预订执行台：可用
- 路书推荐面板：有基础搜索入口，但还需要继续打磨
- 地图主链路：已恢复稳定
- 酒店价格探针：实验版可继续研究

## 本次明确的产品判断

1. 预订主场仍然应该放在路书里。
2. 酒店价格能力必须前置到规划阶段。
3. 推荐能力不能直接替用户给答案，必须先搜，再给候选，再让用户选。
4. 地图主链路不能轻易承载未经验证的新实验，推荐最好先做成侧边工作流，再决定是否进入地图主视图。
